<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <item
            android:id="@+id/item_filtrar_data"
            android:orderInCategory="100"
            android:title="Filtrar por Data"
            app:showAsAction="never" />
        <item
            android:id="@+id/item_filtrar_localizacao"
            android:orderInCategory="200"
            android:title="Filtrar por Localização"
            app:showAsAction="never" />

        <item
            android:id="@+id/item_filtrar_area"
            android:icon="@drawable/ic_cv"
            android:orderInCategory="300"
            android:title="Filtrar por Àrea"
            app:showAsAction="never" />

</menu>